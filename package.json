{
  "name": "nivya",
  "version": "1.0.0",
  "private": true,
  "description": "Nivya Child Safety & Parental Monitoring Platform",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "start": "npm run dev --workspace=web",
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "test": "npm run test --workspace=web"
  }
}
